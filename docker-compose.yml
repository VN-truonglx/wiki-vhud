version: "3.7"

services:
  wiki-vhud:
    build: .
    volumes:
      - .:/wiki-vhud
      - /wiki-vhud/node_modules
      - /wiki-vhud/prisma
    image: wiki-vhud/node:v1
    container_name: wiki-vhud
    ports:
      - "5555:5555"
    restart: unless-stopped
